# AGENTS.md

## 编码前置要求

每次修改程序代码前，先阅读 `docs/DEVELOPMENT_OPERATING_RULES.md`，并确认改动没有破坏其中定义的职责边界。

## 文档写入要求

在执行文档写入之后不要返回检查，也不要渲染。涉及 Word 文档格式时，主要考虑字体、字号、是否加粗、前后段间距、前后行间距、对齐方向、缩进距离、数字和特殊符号是否有特殊字体要求、前后是否要空行。
